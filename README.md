# Steward_A_local_AI_file_assistant
A local file assistant. It runs on your machine, reads your code, tells you what you have, and suggests better names and places for things.  **It cannot delete.** Not "is told not to" — the capability does not exist in the code, and it checks its own source at boot to prove it.
